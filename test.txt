#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <math.h>

char* concat(char* s1, char* s2) {
	char* result = (char*) malloc((strlen(s1)+strlen(s2)+1)*sizeof(char));
	strcpy(result, s1);
	strcat(result, s2);
	return result;
}int foo(intx){
if (x>10) {
	return 1;
}else {
	return 0;
}}

int main(void){
printf("%d", foo(10));
}

